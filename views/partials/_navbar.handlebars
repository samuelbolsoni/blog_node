<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Blog NodeJS</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link {{#ifEquals current_url "/"}} active {{/ifEquals}}" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{#ifEquals current_url "/categorias"}} active {{/ifEquals}}" href="/categorias">Categorias</a>
        </li>
        {{#unless user}}
        <li class="nav-item">
          <a class="nav-link {{#ifEquals current_url "/usuarios/login"}} active {{/ifEquals}}" href="/usuarios/login">Login</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{#ifEquals current_url "/usuarios/registro"}} active {{/ifEquals}}" href="/usuarios/registro">Registro</a>
        </li>
        {{/unless}}
      </ul>
    </div>
    {{#if user}}
      {{>_menu_admin}}
    {{/if}}
  </div>
</nav>